[package]
name = "cross-ctx-note"
version = "0.1.0"

[lib]
kind = "note"
namespace = "miden:cross-ctx-note/miden-cross-ctx-note@0.1.0"
path = "src/lib.rs"

[dependencies]
cross-ctx-account = { path = "../cross-ctx-account" }

[package.metadata.miden.dependencies]
cross-ctx-account = { wit = "../cross-ctx-account/wit/cross-ctx-account.wit" }
